# BIS Conformity Assessment RAG Assistant — optimized production dependencies
# Cloud-native: managed Pinecone + Neo4j. Local storage drivers (ChromaDB and its
# kubernetes/grpc/onnxruntime toolchain) are intentionally excluded per
# spec/reconstruct_docker_image.md.
#
# NOTE: torch is NOT listed here. The Dockerfile installs the CPU-only wheel
# directly from the PyTorch CPU index first, which avoids the multi-GB CUDA
# bundle. sentence-transformers picks up the already-installed torch.

# UI / API framework
streamlit>=1.42.0
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Managed databases
pinecone>=10.0.0
neo4j>=5.15.0

# Embeddings & document parsing
sentence-transformers>=2.3.0
pymupdf>=1.23.0

# LLM synthesis via OpenRouter + config
openai>=1.35.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Retrieval & local knowledge graph
rank-bm25>=0.2.2
networkx>=3.3